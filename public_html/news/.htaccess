<ifModule mod_rewrite.c>
RewriteEngine on
# Public news → Next.js (path /news/{slug} unchanged)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ http://localhost:3000/news/$1 [R=302,L,NE]
</ifModule>
